#include "main.h"
#include <stdio.h>
/**
 * _abs - Prints the absolute value of a number
 * @n: number to be checked.
 * Return: SUccess.
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
