#include "main.h"
#include <string.h>
/**
 * _strcat - It adds one string to another.
 * @dest: The string that we will add a string to .
 * @src: The second string that will be added to another sting.
 * Return: Success.
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
}
