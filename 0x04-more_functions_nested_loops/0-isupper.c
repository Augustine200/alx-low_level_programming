#include "main.h"
/**
 * _isupper - It checks if a letter is upper or lower case
 * @c: The letter to be checked.
 * Return: Success.
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
